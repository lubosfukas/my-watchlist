export const sizeNum = {
    xs: 0,
    sm: 600,
    md: 900,
    lg: 1200,
    xl: 1536,
}

export const size = {
    xs: `${sizeNum.xs}px`,
    sm: `${sizeNum.sm}px`,
    md: `${sizeNum.md}px`,
    lg: `${sizeNum.lg}px`,
    xl: `${sizeNum.xl}px`,
}
